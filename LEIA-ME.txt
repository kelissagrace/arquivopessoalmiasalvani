PHANTASMA // V29

NOVA REGRA:
Se o jogador acertar o próprio PROTEGIDO:
- perde 1 segundo imediatamente;
- HUD atualiza na hora;
- aparece: PROTEGIDO ATINGIDO // -1s

ONDE SUBSTITUIR:
jogo/index.html

AJUSTE MANUAL:
No código procure:

timeLeft = Math.max(0,timeLeft - 1);

Para perder 2 segundos:
timeLeft = Math.max(0,timeLeft - 2);

Para perder 3 segundos:
timeLeft = Math.max(0,timeLeft - 3);
